# ========================================================
# Python/Flask 后端项目 - 环境变量模板
# 团队新成员应复制此文件为 .env, 然后填入自己的配置值。
# 此文件应提交到 Git 仓库。
# ========================================================

# --- 数据库配置 (MongoDB Atlas) ---
# 从 MongoDB Atlas 官网复制的完整连接字符串。
# 需要将 <username>, <password>, 和集群地址等替换为真实信息。
MONGO_URI=

# --- 安全配置 ---
# 用于签发和验证 JWT (用户登录凭证) 的密钥。
# 必须设置为一个长且随机的字符串 (强烈建议32位以上)。
JWT_SECRET=

# --- 服务器配置 ---
# Flask 开发服务器运行的端口。
PORT=5000

# --- Flask 特有配置 ---
# 设置为 'development' 会开启调试模式和自动重载，方便开发。
FLASK_ENV=development

# Flask 应用的入口文件名，用于flask命令。
FLASK_APP=app.py